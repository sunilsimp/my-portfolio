{
  "version": 2,
  "builds": [
    {
      "src": "client",
      "use": "@vercel/node"
    },
    {
      "src": "client",
      "use": "@vercel/static-build",
      "config": { "distDir": "client/build" }
    }
  ]
}
